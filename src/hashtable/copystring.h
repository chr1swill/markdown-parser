#ifndef COPYSTRING
#define COPYSTRING

#include <stdio.h>
#include <string.h>
#include <stdbool.h>

bool copy_string(char* destination_for_string, size_t dest_size, const char* source_string_to_copy);

#endif
